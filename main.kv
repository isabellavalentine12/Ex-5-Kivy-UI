<MainScreen>:
    #: import time __main__.time

    name: 'main'
    #I added the below command while creating my counter
    #i_change_text: i_change_text
    changed_text: changed_text
    counter_button: counter_button #right side references id. Left side is refencered in python file.
    slider: slideer
    alvaro: alvaro

    FloatLayout:
        size_hint: None, None

    Slider:
        id: slideer
        min: 0
        max: 100
        step: 0.0001
        orientation: 'horizontal'
        sensitivity: 'handle'

    DPEAButton:
        text: str(round(slideer.value, 1))
        size: 100, 100
        x: root.width * 0.45
        y: root.height * 0.3
        color: .8, .8, 0, 1
        on_press: root.pressed()

    ImageButton:
        source: '/home/soft-dev/Downloads/alva.jpg'
        size: 90, 90
        x: root.width * 0.81
        y: root.height * 0.05
        on_press: root.admin_action()

    ImageButton:
        id: alvaro
        source: '/home/soft-dev/Downloads/alvaro.jpg'
        size: 90, 90
        x: root.width * 0.65
        y: root.height * 0.05
        on_press: root.animate_alvaro()

    ImageButton:
        source: '/home/soft-dev/Downloads/liz.jpg'
        size: 90, 90
        x: root.width * 0.7
        y: root.height * 0.25
        on_press: root.transition_to_single_button_screen()

    ImageButton
        source: '/home/soft-dev/Downloads/melanie.jpg'
        size: 90, 90
        x: root.width * 0.87
        y: root.height * 0.25
        on_press: root.transition_to_single_button_screen()

    #Label:
        #text: str(round(slideer.value, 3))
        #color: 0, .5, .5, 1

    #I added the following button to create my counter button
    DPEAButton:
        id: counter_button
        text: "0"
        size: 100, 100
        x: root.width * 0.25
        y: root.height * 0.05
        color: .8, 0, .5, 1
        on_press: root.counter()

    DPEAButton:
        id: test_button
        text: 'Off' if test_button.state == 'normal' else 'On' #normal must be lowercase. Single or double quotes don't matter, as long as they're consistent on a word
        size: 100, 100
        x: root.width * 0.05
        y: root.height * 0.05
        color: .8, 0, .5, 1
        on_press: root.pressed()

    DPEAButton:
        #id: i_change_text
        text: 'I Change You ->'
        size: 100, 100
        x: root.width * .15
        y: root.height * .75
        color: 0, .5, .5, 1
        on_press: root.other_button_text_conversion()

    DPEAButton:
        id: changed_text
        text: 'Yeah, right.'
        size: 100, 100
        x: root.width * 0.65
        y: root.height * 0.75
        color: 0, .5, .5, 1
        on_press: root.pressed()

    #SelfUpdatingLabel:
        #id: time_label
        #size_hint: None, None
        #font_size: 30
        #update_property: time.now
        #update_property_parameters: None
        #center_x: root.width * 0.73
        #center_y: root.height * 0.05
        #color: 1, 1, 1, 1
        #bold: True
        #outline_width: self.font_size * 0.1
        #outline_color: 0, 0, 0